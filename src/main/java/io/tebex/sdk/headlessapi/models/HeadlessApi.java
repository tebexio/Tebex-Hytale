package io.tebex.sdk.headlessapi.models;

public class HeadlessApi {
    //    HttpRequest.Builder headlessAuthed(Method method, String endpoint, @Nullable HttpRequest.BodyPublisher data) {
    //        return request(method, formatEndpoint(HEADLESS_API_URL + headlessPrivateKey + "/", endpoint), data)
    //                .header("Authorization", "Basic " + Base64.getEncoder().encodeToString((headlessPublicToken + ":" + headlessPrivateKey).getBytes()));
    //    }
}
